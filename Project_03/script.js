const optionValue = parseInt(document.querySelector('#movie-selector'));
optionValue.addEventListener('click', (e) => {
    // const value = parseInt(document.querySelector('option').value);
    // console.log(e.target.value);
    console.log(optionValue);
    
})

